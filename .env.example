# Frontend base URL for your API
# Production suggestion (Locaweb): keep /api and expose your API in this path or via reverse proxy
VITE_API_BASE_URL=/api

# Dev server proxy target used by Vite (`npm run dev`)
API_PROXY_TARGET=http://localhost:3000
